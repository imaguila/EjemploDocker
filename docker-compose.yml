services:
  backend:
    build: ./backend
    volumes:
      - .:/workspace:cached # Sincroniza el código del Codespace con el contenedor
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    ports:
      - "80:80"